import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OfficeWise AI - Automate the busywork. Ship more. Spend less.",
  description: "OfficeWise helps small and mid-size teams deploy practical AI automations—without the hype. We audit your workflows, integrate the right tools, and ship reliable agents that save hours every week.",
  keywords: ["AI automation", "workflow automation", "business process automation", "AI agents", "productivity tools"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
